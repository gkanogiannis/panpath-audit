# Stabilize machine output

JSON and TSV report typed outcomes, provenance, dual base ledgers, and
case-normalized SHA-256 and BLAKE3 sequence digests. JSON identifies the report
schema and uses the package version as its schema version. Exit codes distinguish
divergence, incomplete correspondence, invalid GFA, and source or operational
failure.
